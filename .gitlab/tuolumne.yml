##############################################################################
# Copyright (c) 2019-2025, Lawrence Livermore National Security, LLC and
# RADIUSS project contributors. See the COPYRIGHT file for details.
#
# SPDX-License-Identifier: (MIT)
##############################################################################

variables:
  TUOLUMNE_JOB_ALLOC: "--queue=pci --exclusive --time-limit=60m --nodes=1 --begin-time=+2s"

.on-tuolumne:
  tags:
    - shell
    - tuolumne
  rules:
    # Runs except if we explicitly deactivate tuolumne by variable.
    - if: '$ON_TUOLUMNE == "OFF"'
      when: never
    # A true statement is expected to allow jobs to run. Here is the default.
    - when: on_success

get-spack-tuolumne:
  extends: [.on-tuolumne, .tuolumne-variables]
  stage: setup
  script:
    - flux watch $( flux batch -o output.stdout.type=kvs ${TUOLUMNE_JOB_ALLOC} .gitlab/scripts/get-spack)
  needs: []

generate-on-tuolumne:
  extends: [.on-tuolumne, .tuolumne-variables]
  stage: generate
  script:
    - .gitlab/scripts/print-variables
    - flux watch $( flux batch -o output.stdout.type=kvs ${TUOLUMNE_JOB_ALLOC} .gitlab/spack/generate-script.sh)
  artifacts:
    paths:
      - "${CI_PROJECT_DIR}/jobs_scratch_dir"
  needs: [get-spack-tuolumne]

build-on-tuolumne:
  extends: [.tuolumne-variables]
  stage: build
  trigger:
    include:
      - artifact: "jobs_scratch_dir/pipeline.yml"
        job: generate-on-tuolumne
      - project: "lc-templates/id_tokens"
        file: "id_tokens.yml"
      - local: ".gitlab/spack/reindex-tuolumne.yml"
    strategy: depend
    forward:
      pipeline_variables: true
  needs: [generate-on-tuolumne]
  rules:
    # Runs except if we explicitly deactivate tuolumne by variable.
    - if: '$ON_TUOLUMNE == "OFF"'
      when: never
    - when: on_success

rm-spack-tuolumne:
  extends: [.on-tuolumne, .tuolumne-variables]
  stage: clean
  script:
    - flux watch $( flux batch -o output.stdout.type=kvs ${TUOLUMNE_JOB_ALLOC} .gitlab/scripts/remove-spack)
  needs: [build-on-tuolumne]
